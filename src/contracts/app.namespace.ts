export namespace AppContract {
  /**
   * Путь к сваггеру
   */
  export const SWAGGER_PATHNAME = "/docs";

  /**
   * Время жизни неактивной сессии (30 дней)
   */
  export const INACTIVE_SESSION_LIFETIME = 1000 * 60 * 60 * 24 * 30;
}
